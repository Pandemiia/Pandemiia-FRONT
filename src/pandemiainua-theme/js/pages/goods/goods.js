import React from 'react';
import { Layout } from '@pinua/common/components';

import styles from './goods.scss';

const Goods = () => {
  return <Layout className={styles.page}>Goods</Layout>;
};

export default Goods;
