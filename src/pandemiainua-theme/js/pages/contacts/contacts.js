import React from 'react';
import { Layout } from '@pinua/common/components';

import styles from './contacts.scss';

const Contacts = () => {
  return <Layout className={styles.page}>Contacts</Layout>;
};

export default Contacts;
