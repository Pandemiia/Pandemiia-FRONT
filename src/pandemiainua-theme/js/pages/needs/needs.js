import React from 'react';
import { Layout } from '@pinua/common/components';

import styles from './needs.css';

const Needs = () => {
  return <Layout className={styles.page}>Needs</Layout>;
};

export default Needs;
