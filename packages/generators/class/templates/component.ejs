import React, { PureComponent } from 'react'
import PropTypes from 'prop-types'
import styles from './<%= name %>.scss'

class <%= namePascalCase %> extends PureComponent {
  static propTypes = {
    history: PropTypes.object
  }

  render() {
    return (
      <div></div>
    )
  }
}

export default <%= namePascalCase %>
