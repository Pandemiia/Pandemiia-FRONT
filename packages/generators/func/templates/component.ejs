import React from 'react'
import PropTypes from 'prop-types'
import styles from './<%= name %>.scss'

const <%= namePascalCase %> = ({ history, ...props }) => (
  <div></div>
)

<%= namePascalCase %>.propTypes = {
  history: PropTypes.object
}

export default <%= namePascalCase %>
